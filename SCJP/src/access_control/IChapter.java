package access_control;

public interface IChapter {

     // must initialize the property
    // the variable must be public static final.
     public static final int cout = 10;

     // method implicitly be public abstract, can't be static
     public abstract void say();
}
